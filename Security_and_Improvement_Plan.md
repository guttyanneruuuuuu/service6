# Pinly 公開前セキュリティ・改善計画

## 概要

本ドキュメントは、`service6` リポジトリで開発されている Pinly アプリケーションの一般公開に際し、セキュリティ対策の強化および全体的な品質向上を目的とした実装計画を提示します。提供された「Pinly_公開前実装チェックリスト.pdf」[1] および既存のコードベースの分析に基づき、主要な改善点を特定し、具体的な対策を立案しました。

## 既存システム分析の要点

Pinly は、登録不要・完全匿名・50字制限の地図SNSであり、クライアントサイドでの動作を基本としつつ、オプションで Supabase を利用したデータ永続化およびリアルタイム同期機能を提供しています。主要な技術スタックは Vite、MapLibre GL JS、バニラJavaScriptです。

コード分析により、以下の主要なコンポーネントとそれに関連するセキュリティ・改善点が特定されました。

*   **`src/data/supabase.js`**: Supabase との連携ロジックを管理。設定は `localStorage` または `window.PINLY_SUPABASE` から読み込まれ、URLとキーの基本的なバリデーションが行われます。しかし、RLSポリシーの定義は `SUPABASE_SETUP.md` に依存しており、現状ではセキュリティが不十分です。
*   **`src/data/moderation.js`**: クライアントサイドでの投稿内容モデレーション（キーワードフィルタリング、個人情報検出、スパム判定）を実装。正規表現ベースのパターンマッチングを使用していますが、サーバーサイドでの追加のモデレーションが推奨されています。
*   **`src/data/ratelimit.js`**: クライアントサイドでの投稿およびレポートのレートリミットを実装。`localStorage` に履歴を保存することでクールダウンを管理しますが、これは容易にバイパス可能であり、サーバーサイドでの対策が必須とされています。
*   **`src/data/store.js`**: Pinly のデータストアを管理。localStorage、Supabase、BroadcastChannel、Seed JSON を統合し、ピンの追加、更新、削除、リアクション処理を行います。Supabase との連携部分で、セキュリティが不十分な RLS ポリシーの影響を受けます。
*   **`index.html`**: アプリケーションのエントリポイント。Content Security Policy (CSP) や X-Content-Type-Options などのセキュリティヘッダーが設定されていますが、今後の機能追加や外部サービス連携に応じて見直しが必要です。
*   **`SUPABASE_SETUP.md`**: Supabase のセットアップガイド。特に、RLS (Row Level Security) ポリシーが匿名ユーザーに対して `pins` テーブルへの完全な `SELECT/INSERT/UPDATE/DELETE` 権限を付与しており、これが本計画における最優先のセキュリティリスクとなります。

## 改善計画

以下の項目について、優先度を考慮しつつ実装を進めます。

### 1. Supabase RLS の強化 (最優先)

**問題点:** `SUPABASE_SETUP.md` [2] に記載されている RLS ポリシーは、匿名ユーザーに `pins` テーブルへの完全な `SELECT/INSERT/UPDATE/DELETE` 権限を与えており、データ改ざんや削除の危険性があります。

**対策:**
*   **匿名ユーザーの `SELECT` 権限:** 既存の `CREATE POLICY 
