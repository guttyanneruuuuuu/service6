# Supabase Realtime 設定ガイド

Pinly でデバイス間のリアルタイム同期を実現するには、Supabase の Realtime 機能を有効にする必要があります。

## 問題の症状

*   デバイス A で投稿したピンが、デバイス B に即座に反映されない
*   ページをリロードすると新しいピンが表示される（Realtime ではなく、手動更新されている）

## 原因

Supabase では、デフォルトでは **Realtime（リアルタイム更新）が無効** になっています。これを有効にするには、Replication 設定で `pins` テーブルを公開する必要があります。

## 解決手順

### 1. Supabase ダッシュボードにアクセス

1.  [Supabase ダッシュボード](https://app.supabase.com/)を開きます。
2.  プロジェクト「service6」を選択します。

### 2. Replication 設定を開く

1.  左メニューの「**Database**」をクリックします。
2.  さらに左メニューから「**Replication**」をクリックします。

### 3. Publication を確認・編集

1.  「**Publication**」セクションが表示されます。
2.  「**supabase_realtime**」という名前の Publication を探します。
3.  その下に「**Tables**」セクションがあります。

### 4. pins テーブルを有効化

1.  「**supabase_realtime**」の右側にある「**Edit**」ボタンをクリックします。
2.  テーブル一覧が表示されます。
3.  **「pins」テーブルを探して、チェックボックスにチェック**を入れます。
4.  チェックボックスの右側に「INSERT」「UPDATE」「DELETE」の 3 つのイベントが表示されます。
    *   **すべてにチェックが入っていることを確認**してください。
    *   入っていなければ、すべてにチェックを入れます。
5.  「**Save**」ボタンをクリックして保存します。

### 5. 設定の反映を待つ

*   設定は通常 **数秒～数十秒以内** に反映されます。

## 動作確認

設定が完了したら、以下の手順で同期が動作しているか確認してください：

1.  **デバイス A** で Pinly を開きます。
2.  **デバイス B**（別のスマートフォン、タブレット、パソコンなど）でも Pinly を開きます。
3.  デバイス A で新しいピンを投稿します。
4.  **デバイス B で、ページをリロードせずに、そのピンが地図上に表示されるか確認**します。
    *   表示されれば、Realtime が正常に動作しています！

## トラブルシューティング

### 「Realtime が動作していない」場合

*   Replication 設定で、「pins」テーブルが「supabase_realtime」に含まれているか再度確認してください。
*   ブラウザのキャッシュをクリアしてから、再度試してみてください。
*   ブラウザのコンソール（F12 キー → Console タブ）を開き、エラーメッセージが出ていないか確認してください。

### 「設定を保存できない」場合

*   Supabase の管理者権限があるか確認してください。
*   ブラウザを再読み込みしてから、再度試してみてください。

## 技術的な背景

Supabase の Realtime 機能は、PostgreSQL の **Logical Replication** を使用しています。Publication に テーブルを追加することで、そのテーブルへの変更がリアルタイムで WebSocket を通じてクライアント（ブラウザ）に配信されるようになります。

Pinly のコード（`src/data/supabase.js`）では、このリアルタイム更新を自動的に受け取り、地図上のピンを即座に更新するようになっています。
