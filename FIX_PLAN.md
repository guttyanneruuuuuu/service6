# 修正方針

## 1. 致命的なエラーの修正
- `src/main.js` 内の変数の二重定義（`lng`, `lat`）を削除し、ビルドエラーを解消します。

## 2. Supabase 統合の実装（最優先事項）
- `src/data/store.js` を修正し、`SUPABASE_SETUP.md` のガイドに従って Supabase との同期機能を実装します。
- `init`, `add`, `react` メソッドを更新し、リアルタイム更新の購読機能を追加します。
- ※ APIキーはプレースホルダーのままですが、ロジック自体を完成させることで、ユーザーがキーを入力すれば動作する状態にします。

## 3. デプロイ設定の改善
- `index.html` で `src/` 配下のファイルを直接参照している箇所を、Vite のビルドに対応した形式に修正します（基本的には現在のままでも Vite が処理しますが、GitHub Pages で `dist` を公開する設定を推奨します）。
- 今回は既存の構成を尊重しつつ、ソースコードの修正をメインに行います。

## 4. チェックリストの更新
- `IMPLEMENTATION_CHECKLIST.md` の「バックエンド永続化」項目を完了としてマークします。
