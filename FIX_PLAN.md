# 修正方針

## 1. 致命的なエラーの修正
- `src/main.js` 内の変数の二重定義（`lng`, `lat`）を削除し、ビルドエラーを解消します。

## 2. Supabase 統合の実装（最優先事項）
- `src/data/store.js` を修正し、`SUPABASE_SETUP.md` のガイドに従って Supabase との同期機能を実装します。
- `init`, `add`, `react` メソッドを更新し、リアルタイム更新の購読機能を追加します。
- ※ APIキーはプレースホルダーのままですが、ロジック自体を完成させることで、ユーザーがキーを入力すれば動作する状態にします。

## 3. デプロイ設定の改善
- `index.html` で `src/` 配下のファイルを直接参照している箇所を、Vite のビルドに対応した形式に修正します（基本的には現在のままでも Vite が処理しますが、GitHub Pages で `dist` を公開する設定を推奨します）。
- 今回は既存の構成を尊重しつつ、ソースコードの修正をメインに行います。

## 4. チェックリストの更新
- `IMPLEMENTATION_CHECKLIST.md` の「バックエンド永続化」項目を完了としてマークします。

## 公開前セキュリティ・改善計画 (2026年5月1日)

### 1. Supabase RLSの強化 (最優先)

**現状:** `SUPABASE_SETUP.md` に記載されているRLSポリシーは、匿名ユーザーに `pins` テーブルへの完全な `SELECT/INSERT/UPDATE/DELETE` 権限を与えている。これは本番環境において重大なセキュリティリスクとなる。

**対策:**
*   匿名ユーザーは `pins` テーブルの `SELECT` のみを許可する。
*   `INSERT` は、`author` が現在のユーザーIDと一致し、かつ `text` や `loc` などのフィールドがクライアントサイドのバリデーションを通過した場合にのみ許可する。
*   `UPDATE` および `DELETE` は、`author` が現在のユーザーIDと一致し、かつ `id` が既存のピンと一致する場合にのみ許可する。
*   `_reported`, `_reportCount`, `_hidden` などの内部管理フィールドは、ユーザーからの直接の `INSERT/UPDATE` を禁止する。

### 2. サーバーサイドレートリミットの実装

**現状:** `src/data/ratelimit.js` にクライアントサイドのレートリミットが実装されているが、これは容易にバイパス可能である。コメントにもサーバーサイドでの対策が必要である旨が記載されている。

**対策:**
*   Supabase Edge Functions を利用して、ピンの投稿 (`INSERT`) およびレポート (`UPDATE`) 時にサーバーサイドでレートリミットを適用する。
*   IPアドレスベース、またはユーザーIDベースでのレートリミットを検討する。
*   クライアントサイドのレートリミットと連携し、ユーザー体験を損なわないように調整する。

### 3. AIモデレーションの強化

**現状:** `src/data/moderation.js` にはクライアントサイドでのキーワードベースのモデレーションが実装されているが、より高度なAIモデレーションが必要である。

**対策:**
*   OpenAI Moderation API などの外部AIサービスをSupabase Edge Functions 経由で利用し、投稿内容の審査を強化する。
*   `SEVERE_PATTERNS` に該当する投稿は自動的に拒否、`WARNING_PATTERNS` に該当する投稿は警告表示、または管理者の承認を必要とするフローを導入する。
*   個人情報が含まれる投稿は自動的に拒否する。

### 4. 環境変数による機密情報の管理

**現状:** SupabaseのAPIキーなどがコード内に直接記述される可能性がある。

**対策:**
*   SupabaseのURLやAnonキー、OpenAIのAPIキーなどの機密情報は、環境変数として安全に管理し、ビルド時に埋め込むか、実行時に安全な方法で取得するように変更する。

### 5. エラーハンドリングとロギングの強化

**現状:** エラーハンドリングが `console.warn` に留まっている箇所がある。

**対策:**
*   本番環境でのエラーを適切に捕捉し、監視できるようなロギングメカニズムを導入する（例: Sentry, CloudWatch Logsなど）。
*   ユーザーへのエラーメッセージをより分かりやすく改善する。

### 6. テストの拡充

**現状:** テストに関する記述が少ない。

**対策:**
*   単体テスト、結合テスト、E2Eテストを導入し、コードの品質と信頼性を向上させる。
*   特にセキュリティ関連の機能（モデレーション、レートリミット、RLS）については、徹底的なテストを実施する。

### 7. CI/CDパイプラインの構築

**現状:** GitHub Pagesへのデプロイは手動またはシンプルなスクリプトで行われている可能性がある。

**対策:**
*   GitHub Actionsなどを利用して、自動テスト、ビルド、デプロイを行うCI/CDパイプラインを構築し、デプロイプロセスを自動化・効率化する。

### 8. ドメイン・ホスティングの改善

**現状:** GitHub Pagesでホスティングされているが、カスタムドメインやより堅牢なホスティングサービスへの移行が検討されている。

**対策:**
*   VercelまたはNetlifyへの移行を検討し、カスタムドメインを設定する。これにより、より高速な配信、SSL証明書の自動管理、容易なCI/CD連携などのメリットを享受できる。

### 9. 利用規約・プライバシーポリシーの最終確認

**現状:** チェックリストでは完了済みとなっているが、上記の実装内容に合わせて文言の最終確認が必要。

**対策:**
*   RLS強化、AIモデレーション、サーバーサイドレートリミットなどの実装内容が、利用規約およびプライバシーポリシーに適切に反映されているかを確認し、必要に応じて更新する。

### 10. 投稿の有効期限機能の確認

**現状:** チェックリストでは完了済みとなっているが、実装の詳細を確認し、意図通りに動作しているか検証する。

**対策:**
*   `store.js` の `EXPIRE_SEC` (48時間) が適切に機能しているか、また、Supabase側での自動削除や非表示処理と連携しているかを確認する。

---
