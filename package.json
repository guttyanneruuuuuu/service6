{
  "name": "pinly",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Pinly — the city's now, pinned to a map. Anonymous, real-time, micro-pins on a live map of the world.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173"
  },
  "devDependencies": {
    "supabase": "^2.98.0",
    "vite": "^6.4.2"
  },
  "dependencies": {
    "maplibre-gl": "^4.7.1"
  }
}
