{
  "name": "zeropoint",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Zeropoint — Teleport to any place on Earth. A digital Anywhere Door.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173",
    "deploy": "vite build"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  },
  "dependencies": {
    "three": "^0.169.0",
    "peerjs": "^1.5.4"
  }
}
